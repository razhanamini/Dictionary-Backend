
services:
  api:
    build: .
    container_name: dictionaty-api-v1
    environment:
      - PORT=3000
      # Set your Neon connection string here. Consider moving this to a secure secrets store or .env.
      - DATABASE_URL=postgresql://neondb_owner:npg_s4xKJM6ZXhUj@ep-frosty-frog-a46vupba-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    ports:
      - "3000:3000"
    restart: unless-stopped

# # Removed local postgres service; using Neon (managed) instead of a local Docker Postgres
# volumes:
#   postgres_data:

